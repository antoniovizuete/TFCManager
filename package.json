{
  "name": "tfcmanager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.14.0",
    "yarn": "1.22.18"
  },
  "scripts": {
    "client": "cd ./frontend && yarn install && yarn build",
    "backend": "cd ./backend && yarn install && yarn start",
    "start": "yarn install && cross-env NODE_ENV=development concurrently --kill-others-on-fail \"yarn backend\" \"yarn client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.1",
    "cross-env": "^7.0.3"
  }
}
